apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webblast-dbs-pvc
spec:
  storageClassName: "gcp-pd"
  accessModes:
    - ReadOnlyMany
    - ReadWriteOnce
  resources:
    requests:
      storage: ${PD_SIZE}
